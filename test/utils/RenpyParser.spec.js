import RenpyParser from "../../app/utils/RenpyParser";
import assert from "assert";

const parser = new RenpyParser();

describe('Parse Test', function () {
    it('should be character', function () {
        assert.equal(true, false);
    });
});